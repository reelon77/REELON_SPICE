# TinySpice 项目梳理与学习计划

> 本文档为个人工作台账，已随仓库跟踪（07-27 起，与 `会话交接/`、`docs/` 一起进仓）。
> 最近更新：2026-08-30（M08 经主会话复验正式销项；全仓 152/152；下一任务等待波形绘图路线选型）
> 完整背景与各次交接文档见 `会话交接/` 文件夹
> **AI 参与记录**：历史细目见 `docs/AI参与记录.md`；2026-08-30 起 AI 获得剩余生产实现授权后的模块/设计/文件主索引见 `docs/AI后续实现台账.md`。
> **工作流**：主会话 = 项目经理（排期/跟进/台账/交接文档），内容工作在其他会话做；任务下发 = `会话交接_<任务>.md`，完成写回 = `回执_<任务>.md`
> **会话命名**：每份任务单必须声明 Task ID 和建议会话名称，格式统一为 `TinySpice｜<Task ID>｜<模块名>`；主会话固定名称为 `TinySpice｜MAIN｜主线管理`。
> **会话边界闸门**：任务会话只能执行“当前活动任务”指向的正式任务单；完成后只可写本任务回执并完成本任务 Git 收口，随后必须停止。即使台账或停车场已写明下游方向，也不得自行选择、拆分、预研、测试或启动下一模块，不得创建下一任务单或修改主线排期。
> **主会话唯一权限**：只有主会话可以正式销项、更新“当前活动任务”、决定排期并提交新的 `会话交接_<任务>.md`；新任务会话在该提交出现前不得开始。
> **当前活动任务**：暂无；最近完成 `TS-M08-OUTPUT`，回执 `会话交接/回执_M08_结果输出与CLI.md`；下一任务待用户选择 Python / MATLAB / 双轨波形路线后由主会话下发。

---

## 一、项目定位（为什么做这个）

- **目标**：一个月做出**迷你 SPICE 电路仿真器**，作为秋招投**华大九天**等 EDA 公司 Engine/求解器 C++ 岗的核心项目。
- **逻辑**：一个项目同时命中"数学（数值方法）+ C++ + EDA（电路仿真）"，把"数学背景 → EDA 底层研发"的桥直接搭起来。
- **项目矩阵**：LSM-Tree = 旗舰系统项目；TinySpice = 数学→EDA 直接证据；Hazel = 可选第 4 周可视化前端（面试称"OpenGL+ImGui 渲染框架"，不称完整游戏引擎）。
- **两条铁律**：
  1. **内核优先于可视化**——宁可"扎实内核 + Python 画图"，不要"半吊子内核 + 花哨界面"。
  2. **边做边投**——第 1 周末纯电阻电路跑通即上简历开投，不等全部完成。

---

## 二、总体架构

### 三层求解器嵌套（核心设计，开发顺序 = 从内往外）

```
瞬态分析 (时间步进：后向欧拉/梯形法)
 └── 牛顿迭代 (非线性方程组求解)
      └── LU 分解 (线性方程组 Ax=b)
```

- 器件库统一 **`stamp()` 多态接口**：每种器件知道如何把自己"盖章"进 MNA 矩阵，新增器件不改求解器 → 可扩展性的核心卖点。
- 内核编译成独立库（`SpiceLib`），CLI/可视化前端只是消费者，**内核绝不反向依赖前端**。

### 目录结构：现状 → 目标

```
现状                          目标（第 4 周形态）
reelons_tiny_spice/           reelons_tiny_spice/
├── CMakeLists.txt            ├── CMakeLists.txt
├── src/                      ├── src/
│   ├── Spice.h/.cpp          │   ├── core/       矩阵、向量、LU（稠密→稀疏CSR）
│   └── (仅 version() 占位)   │   ├── parser/     网表解析、节点映射
├── apps/cli/main.cpp         │   ├── devices/    R/V/I/D/C/L，stamp() 接口
└── build/                    │   ├── mna/        MNA 组装器
                              │   ├── solver/     牛顿迭代、瞬态时间步进
                              │   └── sim/        仿真控制器 .op/.dc/.tran
                              ├── apps/cli/       命令行入口
                              ├── tests/          GTest：解析解 + ngspice 对拍
                              └── scripts/        Python 画波形
```

### 九大模块速览

| # | 模块 | 职责 | 关键点 | 周次 | 状态 |
|---|------|------|--------|------|------|
| 0 | 基础设施 | 矩阵/向量（先稠密后稀疏 CSR 或 Eigen）、CMake、GTest、日志 | CMake + GTest + 稠密 Matrix 已就位 | 1 | ✅ 基本完成 |
| 1 | 网表解析器 | SPICE 文本 → 内部结构，节点名→编号映射 | 数值解析、切词、R/V/I/D/C/L + `.op/.tran/.end` 语义与端到端闭环 | 1 | ✅ 完成（08-26） |
| 2 | 器件模型库 | R/V/I/D/C/L，统一 `stamp()` 多态接口 | 可扩展性核心，面试深挖点 | 1–3 | ✅ R/V/I/D/C/L 完成（08-26） |
| 3 | MNA 组装器 | 遍历器件填 A、b；V/L 通用支路未知量管理 | MNA 扩展维数是常见卡点 | 1–2 | ✅ 完成（08-26，V/L branch 验证） |
| 4 | 线性求解器 | 手写 LU（秀数学）+ 部分选主元；后期稀疏 CSR | 分解 + 求解闭环，32 个单测；稀疏 CSR 留到第 3 周 | 1、3 | ✅ 稠密版完成 |
| 5 | 非线性求解器 | 牛顿迭代 + 二极管 companion model | 对口岗位 JD"非线性方程求解" | 2 | ✅ 完成（08-04，端到端 0.574V 闸门） |
| 6 | 瞬态分析 | 后向欧拉离散 C/L，固定步长时间步进 | RC/RL + `.tran`；梯形法与变步长留停车场 | 3 | ✅ M06 范围完成（08-26） |
| 7 | 仿真控制器 | 编排 `.op/.tran`；`.dc` 待扫描语义与 parser 前置 | 贯穿 | 贯穿 | ✅ M07 完成（08-29） |
| 8 | 结果输出 | 节点/支路标签、CLI、CSV、Python 工具 | 贯穿 | 贯穿 | ✅ M08 完成（08-30，152/152） |
| 9 | 测试体系 | 解析解手推（主打）+ ngspice 对拍 + 边界用例，GTest | 秀数学的地方 | 4 | ⬜ 未开始 |

---

## 三、当前进度（2026-08-29）

**进行中：**
- 暂无。下一任务等待用户选择 Python / MATLAB / 双轨波形路线。

**已完成：**
- [x] CMake 三层结构：根 → `src/`（`SpiceLib` 静态库）→ `sandbox/`（`TinySpice` 可执行），C++23
- [x] 库/可执行分离、`target_include_directories(PUBLIC)` 正确传递头文件路径
- [x] `.gitignore`、`会话交接/` 文件夹约定、占位测试代码已清理
- [x] **Day 1 上午课完成**：电路=图→方程组、KCL、欧姆、分压手推 8V（简化版闸门通过）、电阻/电流源 stamp、G=带权图拉普拉斯。讲义：`会话交接/Day1上午_讲义.md`
- [x] **GTest 接入**（FetchContent v1.17.0），`tests/` 按模块分可执行：`test_matrix` / `test_lu`
- [x] **稠密 `Matrix` 类**：行优先 `std::vector<double>`，构造/`operator()`/`rows()`/`cols()`，5 个单测全过
- [x] **`lu_decomposition_naked`**：不选主元的教学版（保留作数值稳定性对照素材）
- [x] **`lu_decomposition`：带部分选主元的正式版**（07-31）。列优先 Doolittle、就地分解、`|A(i,k)|` 最大者作主元、`perm` 记录换行、主元 < 1e-12 判奇异抛异常。10 个单测全过（含 `|L(i,j)| ≤ 1` 性质、L·U = P·A 精确对齐、未接地拉普拉斯奇异检测）
- [x] **构建链修复**（07-31）：根 CMakeLists 加 `/utf-8`（MSVC 按 936 代码页读 UTF-8 源码，把中文字符串字面量截断，`test_lu` 原本编译不过）+ `CMAKE_EXPORT_COMPILE_COMMANDS ON`（clangd 数据库）+ `scripts/build.ps1` 一键构建
- [x] **`lu_solve`：前代 + 回代**（08-01，自己写）。`y[i] = b[perm[i]]` 重排 → 前代 L·y = P·b（不除 L 对角）→ 回代 U·x = y（除 U 对角）。8 个单测全过。
  - **`lu_solve` 至此，模块 4 线性求解器闭环**：分解 + 求解 + 32 个单测
- [x] **`MnaSystem` 组装器**（08-03，自己写；08-26 泛化）：构造收（含地节点数, 支路未知量数），维数 n = 节点数−1+支路未知量数；`add_to_A/add_to_b` 内部集中处理跳地+减一；`branch_index` 支路下标查询；raw 写入对；const 引用访问器。M06 中 V/L 已共享 branch 空间；概念上把「约束边（B 行 + Bᵀ 列 + 电流乘子）」的 KKT 结构吃透了
- [x] **`Device` 抽象基类 + `Resistor`**（08-03，自己写）：纯虚 `stamp() const` + 虚析构；Resistor 存 g_（构造时换算一次），`conductance_from` 文件局部辅助函数保证校验先于除法；前置声明/include 布局按「.h 轻依赖、.cpp 重依赖」整理
- [x] **`test_mna` 4 用例全绿**（08-03，AI 代写已登记）：落点/符号/对称、**`+=` 卫兵一次通过（Day1 连错三次的弱点补上了）**、跳地、构造校验。总测试 36 个，挂的仍是 naked 版那 2 个
- [x] **技能课(08-04,压缩版约 1h)**:调试五件套(断点/单步/变量面板/watch/调用栈+条件断点)独立复现,launch.json 亲手写;cmake→ninja→clang 三层关系+增量重算 DAG 语义过了主干。压轴练习与 `if(MSVC)` 自答题被用户主动砍,红测试仍红。详见 `回执_技能课_VSCode调试与构建工具链.md`
- [x] **Day 4 热身(08-04)**:LU 接口收敛(砍出参,唯一出口 `LUResult lu_decomposition(const Matrix&)`,16 调用点同步,删 2 个失去被测对象的用例,`deefb93`);两个源极性命名 `node_pos_/node_neg_`、`node_from_/node_to_` + 方向约定入头文件注释(`f6350b1`)
- [x] **🚩 第 2 周里程碑:非线性 DC,08-04 达成(提前一天)**:
  - `Device` 基类加 `stamp(mna, x)` 虚函数默认转发(线性器件零改动,开闭原则落地);`Diode` companion model(g_eq/I_eq,单参 stamp 抛异常拒误用);`Matrix::clear`+`MnaSystem::clear`;`newton_solve`(清零→全体stamp→LU→∞范数,`NewtonResult` 带迭代次数,不收敛抛异常)——**src/ 全部用户自己写**,commits `2902f5e` `5ba92b1` `ca43473`
  - 端到端:V(5V)-R(1k)-D 收敛 **v2 = 0.574191503 V**,初值全 0 实测 **177 次迭代**(预告 178,浮点路径差异);测试 12 例 AI 代写(线性化三点/落点图案/行和为零/累加语义/接口守卫/基类分派/线性烟囱/端到端/不收敛路径),**53 用例 51 绿**
  - 设计决策(面试深挖点,用户能独立复述):杂交虚函数 vs 全体改签名 vs NonlinearDevice 子类 vs variant+concepts 的完整权衡;"雅可比就是 A、求导在设计期、运行期只求值";按引用传工作区的签名语义
- [x] **网表解析器语义层（08-20～08-21）**：`istream` 逐行解析、`.op/.end`、`0/gnd` 节点映射、R/V/I/D 器件构造、带行号的错误路径；两个金标准网表得到 `8V` / `0.574191503V`。四档提交：`afefa72` `842328b` `71b16b2` `cdd3a00`；parser 26/26，全仓 83/83。
- [x] **🚩 M06 瞬态分析（08-21～08-26）**：第 0～5 档全部完成；显式 `TransientContext`、电容后向欧拉 companion、电感 MNA 支路未知量、固定步长轨迹、C/L/`.tran` parser 全部闭环。parsed/manual RC、RL 均有独立手算 oracle；提交 `cd787f0` `638097d` `02c9c9b` `518f3c2` `a281471`，全仓 116/116。详见 `会话交接/回执_M06_瞬态分析.md`。
- [x] **M07 仿真控制器（08-26～08-29）**：无状态 `simulate(const Circuit&)`、variant 结构化结果、`.op/.tran` 调度、全零瞬态初值、异常传播和重复调用闭环；提交 `1abeaa2` `afbb581` `d84af79` `f646425`，全仓 124/124。详见 `会话交接/回执_M07_仿真控制器.md`。
- ⚠️ 遗留教训 1:**stamp 是 `+=` 不是 `=`**(课上连错三次)→ 写 MNA 时第一个单测就测"共享节点电导累加"
- ⚠️ 遗留教训 2:**主元检查的时机**——naked 版在行消元*之前*查 `A(k,k)`,而主元是消元*之后*才变零,所以奇异矩阵漏网、`1/0` 变 inf。列优先循环(k 在外层)天然把"定主元"和"查主元"放在同一时刻。
- ⚠️ 遗留教训 3(08-04 新增):**推导必须过对答数**——g_eq/I_eq 三轮推错(锚点取 0、g_eq 含自由变量、丢 f(k) 项),全部能被"代 0.5 验一下"当场拦截;量纲检查同理(Is²/I 量纲是 A 不是 S)。"人不算数,机器算数,但人必须去对"。
- ⚠️ 遗留教训 4(08-04 新增):**review 意见是清单不是散文**——意见不逐条落实就喊下一轮,当天 3 次(std::exp 提 4 轮才改)。"改A漏B"复现 2 次(较 Day 3 的 4 次收敛):Diode 双参接口没动基类、新增 3 文件没挂 CMakeLists。

**历史遗留状态：**
- [x] naked LU 两个红测试已转为已知缺陷对照用例，全仓首次 53/53（`57960ec`）。
- [x] Day 4 review 的五个小尾巴已全部核销，全仓 55/55（`4309371`）。
- 用户欠账:`docs/AI参与记录.md` 补 08-04 条目(清单在回执第六节)、技能课短笔记 `docs/构建工具链与调试.md`、launch.json 入库决策(.gitignore 负排除)

**下一步（按顺序）：**
- [x] `CurrentSource`（stamp 只碰 b，2 次调用）+ 单测 ✅ 08-03，commit `312276f`
- [x] `VoltageSource`（5 次写入：Bᵀ 列 2 + B 行 2 + b(k)，用 `branch_index` + raw 方法）+ 扩展维单测 ✅ 08-03，commit `c805a29`
- [x] **🚩 第 1 周验证闸门**：✅ **08-03 晚转绿**（v1=10V / v2=8V / i=−2mA 三断言，commit `2d03d4a`），**当晚简历已更新、已开投**。详见 `会话交接/回执_Day3_IV源与闸门8V.md`
- [x] 小清理：`Resistor::stamp` 空操作——核实为**过期台账**，Day2 提交时已清，无此代码
- [x] **技能课** ✅ 08-04 压缩版完成(调试五件套达标;压轴与红测试转换被砍,见回执)
- [x] **第 2 周本职:牛顿迭代 + 二极管** ✅ 08-04 达成,详见上方里程碑条目与 `回执_Day4_牛顿迭代与二极管.md`
- [x] **Day 5 清尾 + 网表解析器**：解析器于 08-21 完成，R/V/I/D + `.op/.end` 和两个金标准网表端到端全绿；详见 `会话交接/回执_Day5续_网表解析器语义层.md`。
- [x] **第 3 周本职：瞬态分析** ✅ 08-26 完成——C/L 后向欧拉、固定步长 RC/RL、`.tran` parser 和独立递推 oracle 全部闭环，全仓 116/116；详见 `会话交接/回执_M06_瞬态分析.md`。梯形法、变步长和输出仍在停车场。
- [x] **M07 仿真控制器**：`.op/.tran` 统一结构化调度、类型安全结果、全零瞬态初值和错误契约已完成；M08 再接 CLI/CSV，`.dc` 继续留停车场。
- [x] **M08 结果输出与 CLI**：Circuit metadata、ostream writer、CLI、stdout/文件输出、三份 examples、CSV、Python `--validate-only` 和 README 均完成；主会话独立复验 152/152。图片渲染工具的官方语言仍可在后续选择，不影响 CSV/CLI 销项。

> **顺序调整说明（08-01）**：原计划是「解析器 → MNA → 闸门」，现调整为「MNA → 闸门 → 简历 → 解析器」。
> 闸门的定义是"分压电路算出 8V"，器件对象在 GTest 里手工构造即可验证，**不需要解析器**；
> 网表文本解析是纯字符串处理，对闸门零贡献。这么换能让简历提前约 1 天投出去，
> 符合本文档第 18 行自己定的铁律"边做边投，不等全部完成"。

---

## 四、四周时间线（对齐日期）

| 周次 | 日期 | 里程碑 | 交付物 | 求职动作 |
|------|------|--------|--------|----------|
| 1 | 07/23 – 07/29 | **纯电阻 .op 跑通** | 解析器 + MNA + 手写 LU + R/V/I，分压电路验证通过 | **周末立即上简历，抢提前批尾声** |
| 2 | 07/30 – 08/05 | **非线性 DC** | 牛顿迭代 + 二极管 companion model，收敛控制（阻尼） | 持续投递，准备港硕留服认证材料 |
| 3 | 08/06 – 08/12（实际 08/26 完成） | **瞬态 M06 范围完成** | C/L 后向欧拉、固定步长、`.tran`、RC/RL 解析递推验证 | 更新简历项目描述 |
| 4 | 08/13 – 08/19 | **工程化 + 深挖** | 稀疏 CSR、GTest 全覆盖、ngspice 对拍、CSV+Python 波形；**有余量才做** Hazel/ImGui 前端 | 面试深挖话术准备，正式批（金九银十）全力投 |

> ✅ **进度预警解除（2026-08-03 晚）**：第 1 周闸门在逾期 5 天后**达成**（v2 = 8V 三断言全绿，commit `2d03d4a`），
> **当晚简历已更新、已开投**——本项目最高优先级求职动作闭环。
> ✅ **第 2 周里程碑达成（2026-08-04，提前一天）**：非线性 DC 全链路（牛顿 + 二极管 companion model），
> 端到端 v2 = 0.574191503V。逾期 5 天开局的进度债就此还清，首次回到计划线**之前**。
> 阻尼/limiting 未实现（有意不扩科，机理已入面试素材）；解析器与 M06 瞬态已于 08-26 完成闭环；第 4 周 Hazel 前端维持放弃。
> **新增硬规则（Day 3 教训）**：执行会话**每阶段绿灯即提交**，不许攒——Day 3 全程 0 主动提交、提醒 6 次无果，
> 且发生过"误删公共模块卫兵"事件，没有中间提交时这类事故的回滚成本不可接受。

**秋招节奏提醒**：提前批 5–8 月，正式批 8–10 月，央国企 9 月密集。港硕三件事别拖：应届认定、留服认证（需 1–2 个月，**现在就启动**）、投递前跟 HR 确认境外硕士可投。

---

## 五、学习内容清单（电路最小知识，翻译成数学语言）

| 知识点 | 数学翻译 | 验证方式 | 状态 |
|--------|----------|----------|------|
| 欧姆定律 / KCL | 线性约束 + 节点流量守恒（∑流出=0） | 手推两电阻分压 | ✅ 07-25，闸门 8V 通过 |
| MNA 改进节点分析 | 把 KCL 组装成 Ax=b；电压源引入拉格朗日乘子式的额外未知量（支路电流） | 手推分压电路 = 8V（**验证闸门**） | ✅ 08-03，端到端 8V 闸门通过；KKT/约束边视角能独立展开 |
| LU 分解 + 选主元 | 高斯消元的矩阵形式，PA=LU | 手推 3×3 与代码对拍 | ✅ 08-01，分解+求解闭环，手推 2×2/3×3/4×4 全部对拍通过 |
| 二极管模型 | 指数型非线性函数 I = Is(e^(V/Vt) − 1) | 单二极管+电阻电路牛顿手算 2 步 | ✅ 08-04,g_eq/I_eq 独立推出(三轮纠错后),对答数纪律立住 |
| 牛顿迭代 / companion model | 非线性器件每步线性化 = 等效电导 + 等效电流源 | 收敛曲线打印 | ✅ 08-04,177 步实测;"三维严格降一维"桥接、消元与牛顿可交换、雅可比=A=灵敏度矩阵,讲义 artifact 存档 |
| 电容/电感 | `i_C=C·dv/dt`，`v_L=L·di/dt` | RC/RL 后向欧拉递推与手算矩阵 | ✅ 08-26，C/L companion 与轨迹闭环 |
| 后向欧拉 / 梯形法 | ODE 隐式离散，微分方程 → 每步一个代数方程组 | RC/RL 离散闭式；RC 减小步长趋近 `e^(−t/RC)` | 🟡 后向欧拉完成；梯形法留停车场 |
| 刚性问题 / 收敛性（进阶） | 隐式方法为何稳定；牛顿不收敛加阻尼/limiting | 面试深挖储备 | 🟡 素材已归档 `docs/面试素材_牛顿收敛性与直流工作点.md`(08-05):凸性⇒Newton–Fourier 全局收敛证明、溢出 vs 发散、多工作点物理性(SRAM 双稳态)、同伦三法(gmin/source/pseudo-transient)、追问预案;阻尼实现未做(有意不扩科) |

---

## 六、参考资料

| 资料 | 用途 | 使用方式 |
|------|------|----------|
| McAllister《Modified Nodal Analysis》PDF | MNA 入门讲义，最先读 | 精读，跟着手推 |
| IIT / TU Delft MNA 课程讲义 | MNA 系统化 + 电压源扩展维 | 精读 |
| **tiny-spice-rs**（GitHub，Rust） | scope 最贴合的开源参考 | ⚠️ **只读思路、看结构，绝不抄代码**：读懂→关掉→自己重写→能解释每一行 |
| ngspice | 复杂电路对拍标准答案 | `brew install ngspice`，作为测试 oracle |
| Eigen | 稀疏矩阵参考（第 3 周可选，先手写） | 对比 API 设计 |
| Google Test | 测试框架 | CMake FetchContent 接入 |
| LSM-Tree 项目自己的 parser | 网表解析器可复用的经验 | 状态机/分词思路迁移 |

---

## 七、设计取舍与红线备忘（面试话术素材）

1. **为什么手写 LU 而不用 Eigen** → 展示数值线代功底；稀疏化是第 3 周的演进故事（稠密 O(n³) → CSR）。
2. **为什么 stamp() 多态** → 器件与求解器解耦，新增 MOSFET 不动内核 → 讲开闭原则。
3. **为什么后向欧拉起步** → 隐式方法 A-稳定，电路方程常刚性；梯形法二阶精度但有振铃，可对比。
4. **验证策略** → 解析解手推为主（秀数学）+ ngspice 对拍（工程严谨性）。
5. **诚信红线** → 参考架构可讲、可坦白"参考了 tiny-spice-rs 的设计"，但每一行代码必须能解释。**凡非自己敲的部分登记在 `docs/AI参与记录.md`，面试前逐条复盘到"能白板重写"为止**。
6. **Hazel 前端** → 只在第 4 周内核达标后做；SPICE 是独立库，引擎只做显示。

---

## 七点五、停车场（明确不排期，08-19 收口后由用户主动拎出）

1. **叙事组合：TinySpice × 实习调度器**——`.dc` 扫描/蒙特卡洛在数学上是独立解 F(x;p)=0，天然并行；实习的 integrate_server（master/worker/Redis/K8s Job，dispatch/retry）正是工业仿真农场（corner/MC 分析）的骨架。面试话术："懂内核 + 懂它上了工业规模长什么样"。届时写进第七节话术清单。
2. **实体组合 demo（1~2 天量级，前置条件：CLI + `.dc` 完成）**：瘦 master 分发 N 个参数点网表 → worker 跑 `tinyspice xxx.sp` → 汇总 CSV 画图；次优变体：同一套东西跑 ngspice 批量对拍回归。

---

## 八、进度日志

| 日期 | 完成内容 |
|------|----------|
| 2026-07-23 | CMake 骨架搭建完成（SpiceLib + TinySpice CLI，C++23）；补 .gitignore；建立本台账；apps→sandbox 改名并修复 clangd 编译数据库；清理编译链占位代码；建 `会话交接/` 约定 |
| 2026-07-25 | Day 1 上午课完成：KCL/欧姆/分压手推 8V 闸门通过，stamp 模式 + G=图拉普拉斯洞察；遗留教训 stamp 累加（`+=`）待单测覆盖 |
| 2026-07-27 | 稠密 `Matrix` 类完成；`lu_decomposition_naked`（不选主元版）写完；GTest 经 FetchContent 接入，`test_matrix`(5) + `test_lu`(9) 建起；台账/交接文档/讲义纳入仓库跟踪 |
| 2026-07-28 ~ 07-30 | 无提交（进度中断 3 天） |
| 2026-07-31 | 修构建链：`/utf-8`（MSVC 936 代码页截断中文字面量，`test_lu` 本来编译不过）、`CMAKE_EXPORT_COMPILE_COMMANDS`、补 `lu_decomposition` 空函数体；完成**带部分选主元的 `lu_decomposition`** + 10 个单测（24 个测试过 22，剩 2 个是 naked 版已知缺陷）。**修正交接单一处错误**：文档称 `[[0.001,−0.001],[−0.001,0.0015]]` 为奇异，实为非奇异（det = 5e−7）——真正的未接地拉普拉斯是 `[[0.001,−0.001],[−0.001,0.001]]`，行和为 0；两者已分别作为奇异/非奇异对照用例入测；建 `docs/AI参与记录.md` 登记 AI 代写部分与复盘清单 |
| 2026-08-01 | **`lu_solve` 自己写完，模块 4 闭环**（32 个单测，`SolveTest` 组 8 个全过）。过程中查出并修掉一个致命 bug：`perm` 完全没被使用，解出来的是 `A·x = P⁻¹b`；另修 3 处工程问题（按值传 `LUResult` 的整矩阵拷贝、缺维度校验、int/size_t 混用）。**又发现交接单一处错误**：`3×3` 用例 `A=[[0,2,1],[1,1,1],[2,0,1]]` 奇异（`r0−2r1+r2=0`）且 `b=[4,6,7]` 不相容，**该方程组无解**，文档声称的 `x=[2,1,2]` 代回第 1 行得 5≠6 —— 已弃用，换成验算过的替代用例。**教训：交接单的手推答案一个都没验算过，一律先自己复核**。 |
| 2026-08-03（晚） | 主会话：Day 2 全部工作提交（95b69de，提交前全量跑测 36 用例仅挂已知 2 个）；下发 Day 3 任务单 `会话交接_Day3_IV源与闸门8V.md`（CurrentSource → VoltageSource → 端到端 8V，时间盒半天，绿灯当晚投简历） |
| 2026-08-03（深夜） | **🚩 第 1 周闸门达成，简历当晚开投**。`CurrentSource`（312276f）+ `VoltageSource`（c805a29，raw 混合坐标自写）+ 端到端 8V 三断言（2d03d4a），43 用例 41 绿。插曲：开场曾有 agent 代写代码，用户主动叫停并 reset 重写（诚信红线自我执行 ✓）。暴露问题：①"改A漏B"复现 4 次（含误删公共卫兵的恶性变体）；②全程 0 主动提交 → 升级硬规则"每阶段绿灯即提交"。物理认知补漏：电压源焊差值、电势是联立解。详见 `回执_Day3_IV源与闸门8V.md` |
| 2026-08-04 | **🚩 第 2 周里程碑提前一天达成:非线性 DC 全链路**。上午技能课(压缩版,调试五件套独立复现);热身清 Day 3 技术债(LU 接口收敛 `deefb93`、极性命名 `f6350b1`);正餐:Device 双参虚函数默认转发 + Diode companion model(`2902f5e`)、clear(`5ba92b1`)、newton_solve(`ca43473`),src/ 全部用户自己写,端到端 v2=0.574191503V/177 次迭代,53 用例 51 绿。**提交纪律 5 阶段 5 提交零攒批**(git 由 AI 代敲)。新教训:推导必须过对答数(三轮推错全可被拦)、review 意见逐条落实(std::exp 提 4 轮)。"改A漏B" 2 次(收敛中)。两份讲义 artifact(牛顿数学案例+求解器施工图)。回执 ×2 已归档(`6f8f496`) |
| 2026-08-20～08-21 | **网表解析器模块完成**：主循环、`.op/.end`、节点映射、R/V/I/D 语义、错误行号和两个端到端网表全部落地；用户亲手编写 parser 核心，AI 代写语义/端到端测试并登记。四档四提交，parser 26/26，全仓 83/83；回执 `回执_Day5续_网表解析器语义层.md`。 |
| 2026-08-21～08-26 | **M06 瞬态分析 P0/P1 完成**：显式历史上下文、Capacitor、Newton 瞬态单步、固定步长 RC、Inductor/RL、C/L/`.tran` parser 依次完成；5 个阶段提交，新增 33 个测试，所有数值用例均使用独立手算/公式 oracle，全仓由 83/83 增至 116/116；回执 `回执_M06_瞬态分析.md`。 |
| 2026-08-26 | **下发 M07 仿真控制器**：基于已完成的 parser、Newton 和 transient 建立 `.op/.tran` 统一入口；首档为返回类型、所有权和零初值策略设计闸门，CLI/CSV 与 `.dc` 分别留给下游。 |
| 2026-08-29 | **M07 仿真控制器完成**：无状态 `simulate(const Circuit&)` 统一调度 `.op/.tran`，variant 返回工作点或轨迹；全零瞬态初值、None/异常传播、重复调用和 parsed 分压/二极管/RC/RL 全部通过，新增 8 个测试，全仓 124/124。 |
| 2026-08-29 | **主会话正式验收 M07 并下发 M08**：独立复验 main/origin 同步、工作区干净、controller 8/8、全仓 124/124；将任务会话越级预研事件固化为主会话唯一销项/排期/下发权限，并创建 `TS-M08-OUTPUT`。 |
| 2026-08-30 | **主会话正式验收 M08**：main/origin 同步、工作区干净、全仓 152/152；`.op`、RC/RL CSV 与 Python `--validate-only` 实跑通过。建立 `docs/AI后续实现台账.md`，从本日起透明记录 AI 获得生产实现授权后的模块设计、文件、测试与提交；下一任务等待波形工具选型。 |
| 2026-08-03 | **模块 2/3 主体落地**：`MnaSystem`（跳地+减一集中处理、branch_index、raw 写入）+ `Device` 抽象基类 + `Resistor`（含构造校验），全部自己写；`test_mna` 4 用例全绿（AI 代写已登记进 AI参与记录），**`+=` 卫兵一次通过**。修 CMakeLists 账目（删 Device.cpp 残留、补 Resistor）。学习收获：MNA 的 KKT/约束边视角、A(i,j)=∂f_i/∂x_j、explicit/内部链接/初始化列表时序等 C++ 工程惯用法。闸门只剩 I/V 源 + 端到端 |
